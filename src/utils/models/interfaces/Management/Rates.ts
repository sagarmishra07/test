export interface IRates{
  id: number;
  category: string;
  amount: number;
  basis: string;
  rateCategory: boolean;
}
export interface IRatesDTO{
  id: number;
  category: string;
  amount: number;
  basis: string;
  rateCategory: boolean;
}
